import React from 'react';
import './LeftDetail.css'; // Make sure to create a corresponding CSS file for styling

const LeftDetail = () => {
    return (
        <div className="left-detail">
            {/* Add content similar to the sidebar shown in the screenshot */}
            {/* This is a placeholder, you would replace it with actual links or content */}
            <div className="menu-item">OpenAI</div>
            <div className="menu-item">Coming Soon</div>
            <div className="menu-item">Model Admin</div>
            <div className="menu-item">Inference Endpoint</div>
        </div>
    );
};

export default LeftDetail;